"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createCursor = void 0;
const cli_cursor_1 = __importDefault(require("cli-cursor"));
/**
 * Returns an object capable of showing and hiding the CLI cursor.
 * @remarks This is a thin wrapper around {@Link https://www.npmjs.com/package/cli-cursor cli-cursor}.
 */
function createCursor() {
    let isHidden = false;
    const hide = () => {
        if (!isHidden) {
            cli_cursor_1.default.hide();
            isHidden = true;
        }
    };
    const show = () => {
        if (isHidden) {
            cli_cursor_1.default.show();
            isHidden = false;
        }
    };
    return { hide, show, isHidden: () => isHidden };
}
exports.createCursor = createCursor;
exports.default = createCursor;
